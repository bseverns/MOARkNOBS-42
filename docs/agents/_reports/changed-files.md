- firmware/include/EnvelopeFollower.h — reshape the EfStats struct to match the requested diagnostics payload (float value + mode ID).
- firmware/src/EnvelopeFollower.cpp — populate EfStats with the baseline, gain, converted value, and uint8 mode flag used by bench prints.
- firmware/src/firmware_main.cpp — move EF sample updates onto the shared 1 kHz tick, cache the levels, and keep the mid-priority logic reusing the cached data.
- firmware/test/test_envelope_follower.cpp — add synthetic sine, step, and idle noise traces that exercise the new stats reporting paths.
- firmware/test/test_mainUnity.cpp — declare and register the new envelope follower tests with the Unity runner.
- firmware/test/test_ef_modes.cpp — adjust the stats-mode assertion to the uint8_t mode field.
- App/runtime.js — enforce single-flight JSON-RPC queuing with throttled writes, 3 s timeouts, and automatic rollback whenever a request fails.
- App/tests/rpc_queue.spec.js — quantify throttle timing under a slow simulator response so the queue honors single-flight sequencing before honoring timeouts.
- firmware/include/ClockDiscipline.h — introduce the PPQN-derived clock discipline helper that tracks running state, drift, and ms-per-tick.
- firmware/src/ClockDiscipline.cpp — implement the helper with resume/drift detection, smoothing, and tick consumption so callers get tidy deltas.
- firmware/include/Arpeggiator.h — plug the arpeggiator into the helper so it can reset on start/resume and read tempo estimates from one place.
- firmware/src/Arpeggiator.cpp — rewrite update() to consume ticks via ClockDiscipline, reset the groove on resumes/drift, and lean on its ms-per-tick for timing math.
- firmware/test/test_clock_ppqn.cpp — oxygenate the clock discipline with start/stop/continue coverage plus a long jittery run that enforces the ±1 ms accuracy and tempo-jump resync.
- firmware/test/test_mainUnity.cpp — declare and run the new clock-discipline tests so the unified suite stays comprehensive.
- firmware/include/Arpeggiator.h — expose the new `msPerTickEstimate()` helper so tests can inspect the clock-derived tempo without poking private fields.
- firmware/test/test_arpeggiator.cpp — update `test_tempo_change_updates_tick_ms()` to use the public helper instead of the removed `_msPerTick` member.
- firmware/platformio.ini — include `ClockDiscipline.cpp` in every test-specific `build_src_filter` so the new helper links into the various test targets.
