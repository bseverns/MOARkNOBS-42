# Hardware-Test Matrix

| Test name                       | Required hardware                                                                             | Firmware env                  | Command                                                                                                                                                                                                                                                                               | Expected result                                                                   | Pass/fail notes                                                  | Known limitations                                      |
| ------------------------------- | --------------------------------------------------------------------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------------ |
| Baseline firmware build         | host with PlatformIO                                                                          | `teensy40_main`               | `pio run -d firmware -e teensy40_main`                                                                                                                                                                                                                                                | clean build                                                                       | pass if compile completes                                        | build only; no hardware proof                          |
| Unity smoke suite               | Teensy 4.0 on USB                                                                             | `teensy40_unity`              | `pio test -d firmware -e teensy40_unity -vvv`                                                                                                                                                                                                                                         | custom Unity suite passes over Serial1 transport                                  | fail if PlatformIO autogen Unity transport appears or tests fail | logic-focused; not full production boot path           |
| Unified bench bring-up          | assembled prototype                                                                           | `teensy40_unified_test`       | `pio run -d firmware -e teensy40_unified_test -t upload`                                                                                                                                                                                                                              | manual checks for buttons, pots, LEDs, OLED, EF path                              | operator confirms prompted checks                                | manual lane                                            |
| OLED and LED exerciser          | assembled prototype with OLED and LEDs                                                        | `teensy40_display_led_hw`     | `pio run -d firmware -e teensy40_display_led_hw -t upload`                                                                                                                                                                                                                            | OLED animates, LED phases run, no `0x3C` init failure                             | fail if OLED does not init or LED phases are incomplete          | not a full-system lane                                 |
| Button plus EF USB MIDI demo    | control button, EF input, USB host                                                            | `teensy40_button_ef_demo`     | `pio run -d firmware -e teensy40_button_ef_demo -t upload`                                                                                                                                                                                                                            | USB MIDI note and CC traffic appear on host                                       | fail if button or EF traffic is missing                          | reduced lane; not full runtime                         |
| Filter verification             | prototype or lab target                                                                       | `teensy40_biquad_test`        | `pio run -d firmware -e teensy40_biquad_test`                                                                                                                                                                                                                                         | filter-focused build compiles for calibration/inspection                          | pass if build succeeds and test target is usable                 | not a complete hardware bring-up story                 |
| EEPROM persistence              | assembled prototype with reset access                                                         | `teensy40_eeprom_persistence` | `pio run -d firmware -e teensy40_eeprom_persistence -t upload`                                                                                                                                                                                                                        | staged save, reboot verify, backup-restore pass                                   | pass if `Primary load PASS` and `Backup restore PASS` appear     | intentionally mutates stored config during test        |
| Slot storage verification       | assembled prototype                                                                           | `teensy40_slot_verify`        | `pio run -d firmware -e teensy40_slot_verify -t upload`                                                                                                                                                                                                                               | addressable slot storage verifies                                                 | pass if slots print `PASS` and final summary passes              | depends on target EEPROM capacity                      |
| Power and LED burn-in           | assembled prototype, stable bench supply recommended                                          | `teensy40_power_burnin`       | `pio run -d firmware -e teensy40_power_burnin -t upload`                                                                                                                                                                                                                              | load phases run without resets or brownout growth                                 | fail on reset, LED corruption, or unstable `vref` behavior       | requires human interpretation of long soak behavior    |
| Full-stack bridge lane          | assembled prototype, USB serial, host with Node                                               | legacy raw-OSC lane           | `node firmware/system_test/mn42_fullstack_runner.js --serial /dev/ttyACM0 --report logs/system-test.json`                                                                                                                                                                             | legacy bridge OSC heartbeat and command loop succeed                              | fail on timeout or routing error                                 | older lane; no longer the primary bridge-runtime proof |
| Structured bridge-session proof | assembled prototype already running configurator-capable firmware, USB serial, host with Node | `teensy40_main`               | `node firmware/system_test/mn42_boot_contract_runner.js --serial /dev/cu.usbmodemXXXX --attach-live --report logs/boot-contract-attach-live.json` then `node firmware/system_test/mn42_bridge_session_runner.js --serial /dev/cu.usbmodemXXXX --report logs/bridge-session-test.json` | bridge server session becomes ready, staged apply ACKs, cleanup restores baseline | fail on HTTP/session timeout, bad ACK, or cleanup mismatch       | primary HIL proof for the upgraded bridge runtime      |
| Production boot contract        | assembled prototype, USB serial, host with Node                                               | `teensy40_main`               | `node firmware/system_test/mn42_boot_contract_runner.js --serial /dev/cu.usbmodemXXXX --flash --report logs/boot-contract.json`                                                                                                                                                       | standalone boot, configurator reboot, handshake, apply/ACK                        | fail on missing boot marker, handshake timeout, or bad ACK       | leaves board in configurator mode until next reset     |
| Attach-live config contract     | assembled prototype already running firmware, USB serial, host with Node                      | `teensy40_main`               | `node firmware/system_test/mn42_boot_contract_runner.js --serial /dev/cu.usbmodemXXXX --attach-live --report logs/boot-contract-attach-live.json`                                                                                                                                     | live HELLO, configurator reboot, handshake, apply/ACK                             | fail on serial reconnect, handshake timeout, or bad ACK          | does not prove the cold-boot standalone banner         |
| Browser configurator check      | assembled prototype, Chromium-based browser                                                   | `teensy40_main`               | `python3 -m http.server --directory App 8000` then open `http://localhost:8000/`                                                                                                                                                                                                      | WebSerial connect, manifest/config fetch, live telemetry                          | fail if `HELLO` or config flow fails                             | browser support is intentionally narrow                |
| Bridge automated tests          | host with Node 24                                                                             | n/a                           | `npm --prefix bridge test`                                                                                                                                                                                                                                                            | bridge CLI, transport, and browser-server tests pass                              | fail on any JS suite failure                                     | host-only; no hardware timing proof                    |
| App automated tests             | host with Node 24 and Playwright browser                                                      | n/a                           | `npm --prefix App test`                                                                                                                                                                                                                                                               | App simulator and transport tests pass                                            | fail on any Playwright regression                                | host-only; no live-device proof                        |
