[platformio]
; Top-level shim so an accidental build from repo root still finds headers
include_dir = firmware/include
src_dir = firmware/src
test_dir = firmware/test
extra_configs = firmware/platformio.ini
