[platformio]
; This repository's PlatformIO project root is `firmware/` only.
; Running `pio` from repo root is intentionally blocked to avoid split-truth builds.
default_envs = firmware_project_only

[env:firmware_project_only]
platform = native
extra_scripts = pre:scripts/reject_root_pio.py
